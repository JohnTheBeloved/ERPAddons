<odoo>

    <!-- <record id="view_product_group" model="ir.ui.view">
        <field name="name">product_group.tree</field>
        <field name="model">website_utils.product_group</field>
        <field name="arch" type="xml">
            <form>
                <field name="group_name" />
                <field name="query_id" />
                <field name="product_ids" widget="one2many_list" />
            </form>
        </field>
    </record> -->
    <record id="product_group_line_tree" model="ir.ui.view">
        <field name="name">product.group.line.tree</field>
        <field name="model">website_utils.product_group</field>
        <field name="arch" type="xml">
            <tree string="Product Group Lines" create="true">
                <field name="id"/>
                <field name="group_name"/>
            </tree>
        </field>
    </record>

</odoo>